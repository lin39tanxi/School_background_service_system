<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.we_are_team.school_background_service_system.mapper.FeedbacksMapper">
<select id="getList" resultType="com.we_are_team.school_background_service_system.pojo.vo.GetFeedbackVO">
    SELECT f.feedback_id, f.content, f.is_anonymous, f.created_time, f.image_urls ,u.student_number FROM feedback as f
    LEFT JOIN user as u ON f.user_id = u.user_id
    <where>
        <if test="userId != null">
            f.user_id = #{userId}
        </if>
        <if test="orderKey != null">
            and (f.feedback_id LIKE CONCAT('%', #{orderKey}, '%') )
        </if>
        <if test="beginTime != null">
            and f.created_time >= #{beginTime}
        </if>
        <if test="endTime != null">
            and f.created_time &lt;= #{endTime}
        </if>
    </where>
    order by f.created_time desc
</select>
</mapper>